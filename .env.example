# Karakeep Configuration
KARAKEEP_URL=https://karakeep.yourdomain.com
KARAKEEP_API_KEY=your_api_key_here
# Optional: Public URL for email footer links (if different from API URL)
# KARAKEEP_PUBLIC_URL=https://karakeep.yourdomain.com

# LLM Configuration (choose one)
# Option 1: Anthropic Claude (recommended)
ANTHROPIC_API_KEY=sk-ant-...

# Option 2: Local Ollama
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3

# SMTP Configuration
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=re_...
# SMTP_SECURE is auto-detected based on port if not set:
#   Port 465 = true (implicit TLS)
#   Port 587/25 = false (STARTTLS, auto-upgraded)
# Only set explicitly if you need to override:
# SMTP_SECURE=true

# Email Addresses
EMAIL_FROM=digest@yourdomain.com
EMAIL_TO=you@example.com
# Multiple recipients: EMAIL_TO=user1@example.com,user2@example.com

# Digest Configuration
# Priority tags get boosted in the Quick Scan section
PRIORITY_TAGS=important,work,reference

# Run mode: cli (single run) or daemon (scheduled)
RUN_MODE=cli

# Cron schedule for daemon mode (default: Sunday 8am)
# Format: minute hour day-of-month month day-of-week
CRON_SCHEDULE=0 8 * * 0
